@page
@model dotNetZadanie3.Pages.People.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Years[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Years[0].Surname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Years[0].Year)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Years[0].date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Years[0].wynik)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Years) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Surname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Year)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.wynik)
            </td>
            <td>
               <!--  <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> | -->
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

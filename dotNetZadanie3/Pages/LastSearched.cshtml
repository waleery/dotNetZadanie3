@page
@model dotNetZadanie3.Pages.LastSearchedModel
@{
}
<p>Ostatnie 20 wyników</p>

@if (Model.YearsContext != null){
    @foreach (var item in Model.YearsContext)
    {
        <p>@Html.DisplayFor(modelItem => item.wynik)</p>
    }
}